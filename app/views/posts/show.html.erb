<h1><%= @post.title %></h1>

<p><%= @post.body %></p>
<p><%= image_tag @post.image.url(:large), class: 'img-show' if @post.image? %></p>

<h2>Comments</h2>
<%= render @post.comments %>

<h2>Add a comment:</h2>
<%= render 'comments/form' %>

<%= link_to 'Edit', edit_post_path(@post) %> |
<%= link_to 'Back', posts_path %>